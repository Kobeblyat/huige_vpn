name: ssrvpn_windows
description: 灰哥VPN - 安全稳定的VPN客户端 (Windows 安装版)
publish_to: 'none'
version: 4.0.16+4016
resolution: workspace

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.1.0

  # YAML parsing for Clash config
  yaml: ^3.1.0

  # HTTP client for subscription fetch and Clash API
  http: ^1.0.0

  # Current-user DPAPI storage with same-directory atomic replacement
  ffi: ^2.1.0
  win32: ^6.4.0

  # Shared code across all SSRVPN platforms
  ssrvpn_shared:
    path: ../packages/ssrvpn_shared

  # System tray for Windows
  system_tray: ^2.0.3

  # Window management
  window_manager: ^0.5.1
  screen_retriever: ^0.2.1

  # Path utilities
  path: ^1.9.0

  # Asset integrity checks
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/geoip.metadb.gz
    - assets/icon.ico
