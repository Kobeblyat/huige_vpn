name: ssrvpn_shared
description: Shared SSRVPN models and services.
publish_to: 'none'
version: 1.0.0
resolution: workspace

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  characters: ^1.4.0
  country_flags: ^4.0.0
  crypto: ^3.0.0
  flutter:
    sdk: flutter
  http: ^1.0.0
  meta: ^1.12.0
  uuid: '>=3.0.6 <5.0.0'
  yaml: ^3.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  test: ^1.24.0

flutter:
  uses-material-design: true
